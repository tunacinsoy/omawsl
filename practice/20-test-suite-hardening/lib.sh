#!/usr/bin/env bash
# practice/20-test-suite-hardening/lib.sh
#
# Rebuild the two testable fixes from this phase. See
# docs/curriculum/20-test-suite-hardening.md, section 3 (Exercise), for the
# exact function names, argument orders, and required behavior of each one.
#
# This file is meant to be *sourced*, not executed directly - it should
# define functions only, with no top-level code that runs on its own, and
# no auto-run dispatcher at the bottom.
#
# Functions to implement here:
#   - omawsl_zoxide_doctor_should_suppress <starship_present>
#   - omawsl_scoped_mktemp_dir <base_dir> <label>
#
# TODO: implement each function described above.
